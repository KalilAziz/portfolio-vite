//const diretorio = window.location.pathname;
export const dirInicial = {
  inicio: window.location.pathname == '/',
  projeto: window.location.pathname == '/projetos',
  sobre: window.location.pathname == '/sobre',
  suporte: window.location.pathname == '/suporte',
};
