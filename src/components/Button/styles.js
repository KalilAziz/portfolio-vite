import styled from 'styled-components';

export const Button = styled.button`
  color: white;
  background: var(--Gradient);
  border: none;
  padding: 15px 40px;
  border-radius: 30px;
  margin: auto 0;

`;
